第一题：

加密位置为“

```
so(Yr, Vr, Xr)
```

![image-20250607103513989](assets/image-20250607103513989.png)



其中Yr 变量是通过一个嵌套函数调用生成的，这个过程涉及到文本编码、数学运算和字符串操作



```
new TextEncoder()['encode']
```

```
Yr = new TextEncoder()[n](
    (Math[f]()[i](t) + r)[u](e, b) + 
    o + 
    Date[s](c) + 
    l
);
```

```
Math[//▼・ᴥ・▼]

                                                                                                    f]()
为 Math["random"]()
```

```
(Math["random"]()['toString'](36) + '0000000000000000')['slice'](2, 18) +'咦~，可带劲~。' +1749263909607 + '俺不中嘞~'
```

```
new TextEncoder()['encode']((Math["random"]()['toString'](36) + '0000000000000000')['slice'](2, 18) +'咦~，可带劲~。' +Date.now() + '俺不中嘞~')
```

