无限debugger:

```undefined
(function anonymous(
) {
debugger
})
```


```undefined
(function (){
    var open = window.XMLHttpRequest.prototype.open;
    window.XMLHttpRequest.prototype.open = function(method,url,async){
        if (url.indexOf('hHp4Kgam') != -1) {
                debugger;
        }
        return open.apply(this,arguments)

    }
})();
```

```undefined
(function (){
    var open = window.XMLHttpRequest.prototype.open;
    window.XMLHttpRequest.prototype.open = function(method,url,async){
        debugger;
        return open.apply(this,arguments)

    }
})();
```

```undefined
(function (){
    var open = window.XMLHttpRequest.prototype.open;
    window.XMLHttpRequest.prototype.open = function(method,url,async){
        console.log(url)
        return open.apply(this,arguments)

    }
})();
```


```undefined
xml = new XMLHttpRequest()
xml.open("post", 'https://cpquery.cponline.cnipa.gov.cn/api/view/gn/fwxx')
```



```undefined
managed_fund  需要修改
```


如何定位加密位置：

hook脚本

xhr断点、

关键字搜索

开源项目比如


主要看数据量，反爬更新频率、


python基础：

```undefined

```


# 数据库优化

```undefined

```


# JS基础

```undefined

```


# 验证码破解：


# JS加密：


# ip池


# cookie池


# 代理池


# 指纹浏览器


# Webpack


# JSVMP


# 标准算法加密


# 大厂加密


# 风控

一、IP风控

## 二、设备指纹风控

## 三、账号风控

三、



```undefined
逆向文章：
https://www.52pojie.cn/thread-2069036-1-1.html
```


五天时间把过往的所有代码整理，主要工具使用



```undefined
岗位职责:
1、负责 Web端协议分析&参数逆向，完善解决方案;
2、持续迭代端上攻防能力，探索建设全自动化的对抗体系。
技能要求(符合部分即可):
精通 JavaScript逆向，具备 AST语法树分析能力(BabelParser)，熟悉常见 JS混淆(字符串加密、控制流平坦化)及反混淆技巧;
精通浏览器调试(Chrome DevTools)、自动化(Puppeteer/Playwright)，有Chromium内核魔改经验更佳;
熟悉 Web端反爬对抗(浏览器指纹&行为、TLS指纹等等)、人机识别对抗(点选/滑块验证码、reCaptcha、Akamai等);
熟悉常见应用层协议(HTTP/Websocket)，具备网络
抓包分析能力)Charles/Wireshark);
熟悉常见加密算法(RSA/AES/HMAC)及接口签名机制;
精通 web安全，有OWASP Top10漏洞的挖掘&利用经验，熟悉 WAF等安全防护;
·具备 Pvthon/Node.s开发能力，能独立编写自动化工具
```