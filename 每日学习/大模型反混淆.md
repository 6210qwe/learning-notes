2025-7-7

```

ast+补环境比较好
231昨天下午rand更新了
1688 和231并列的一个参数，1688详情就有， rand参数

字节的strdata这个参数怎么搞：
如果token过期比较快，就要搞这个参数
完全是流程就是，如果没有mstoken，
就先请求一次，没有任何参数,然后带上strdata请求第二次，
然后拿到mstoken，在带上stadata在请求第三次
tk是要逆这个，字节不是刚需


1、收集常见的加密代码
2、JS和python基础和进阶学习
3、整理成自己的网站
4、写一个大模型还原混淆JS的项目
5、写一个大模型自动生成爬虫脚本的项目
6、

1、浏览器加载页面，访问url需要时间
2、
现在从访问验证码到识别出验证码: 需要11秒

自动化：
1、浏览器访问验证码地址 1-7秒
2、判断到底是普通还是水果滑块验证码 10秒
3、打码平台识别需要3秒
4、拖动滑动滑块验证成功：需要三秒


为什么drissonpage过滑块的时候，浏览器不在最前面，它就不滑动


AI还原混淆代码遇到的问题：
1、代码太长的话，模型输出的是被截断的
2、


请提供：
1. 代码片段或文件
2. 具体需求（如："提取加密字符串" / "还原控制流"）
3. 遇到的错误（如有）

我会给出：
✓ 可读性重构
✓ 混淆技术分析
✓ 逐步解密方案

注意事项
性能提示：完整处理80KB文件可能需要15-25轮交互
精度控制：建议优先处理关键函数（如加密/通信模块）
验证要求：对还原后的代码建议用esprima验证语法完整性


```

2025-7-8

```
1、代码实现历史记忆和多轮对话
2、实现比赛代码的完整解析
3、封装现有的模型调用方法

问题：
1、上下文长度  输入120kb  输出 16kb
2、会被知识产权限制[捂脸]DeepSeek不愿帮人还原混淆代码，说这是违法的。我说是自己写的，它就省略好多关键代码
3、代码流式输出
4、


使用AST过掉 document.all的检测，有没有搞头?
js逆向补环境遇到了 document.all
https://www.52pojie.cn/thread-1903924-1-1.html

项目：
1、大模型补环境
2、大模型生成爬虫脚本
3、大模型实现纯算
4、大模型实现代码反混淆


1、实现支持各种大模型
2、实现完整的代码解混淆
3、支持多轮对话，

 self.system_prompt = """
            你是专业的JavaScript反混淆工具，请对提供的混淆代码进行处理：
            包括但不限于
            1. 保持原始代码的函数声明形式（优先使用function关键字）。
            2. 合并被分割的字符串，保持日志信息的完整性。
            3. 不要改变原始代码的逻辑顺序。
            3. 保留原始console方法的类型（如log→log，warn→warn，debug→debug）。
            4. 如果遇到字符串被分割的情况，尝试合并为一条完整信息。
            5. 输出代码应忠实反映原始逻辑，不添加或减少功能
            4. 还原变量名、函数名（使用有意义的名称）
            5. 解密加密字符串、展开数组引用
            6. 简化控制流（去除冗余跳转、扁平化结构）
            7. 如果有vmp虚拟机混淆加固的话，也需要还原
            8. 保留原始功能逻辑，只优化可读性
            9. 输出整理后的完整代码，不添加额外解释
            10. 需要确保还原的代码可以正确执行和完整性
            11. 需要标明代码使用了那些混淆方式
            12. 不要省略任何步骤
        """
        
        
    # self.system_prompt = """
    # 你是专业的JavaScript反混淆专家，严格按以下步骤处理代码：
    # 1. **分析阶段**：
    #    - 识别混淆类型, 包括但不限于（如字符串分割、控制流扁平化、VMP、ES5转换、防格式化字符、逻辑表达式混淆、常量分解、控制变量混淆、字符串加密、内存爆破、数组引用等）
    #    - 输出格式："已识别混淆类型：[类型列表]"
    #
    # 2. **还原阶段**：
    #    - 基于你分析出来的JS代码混淆类型进行代码还原
    #    - 不要改变原始代码的逻辑顺序
    #    - 输出代码应忠实反映原始逻辑，不添加或减少功能）
    #    - 还原变量名、函数名（使用有意义的名称）
    #    - 保留原始功能逻辑，只优化可读性
    #    - 确保你还原后的代码没有任何混淆
    #
    # 3. **验证阶段**：
    #    - 需要确保还原代码与原始功能一致
    #    - 需要确保还原的代码可以正确执行和完整性
    #    - 不要省略任何步骤"
    # """
    
```


